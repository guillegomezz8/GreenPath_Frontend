services:
  react_project:
    container_name: greenpath_frontend
    image: react_project:dev
    build: .
    ports:
      - "5173:5173"
    command: npm run dev
    volumes:
      - .:/app
      - /app/node_modules
